services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.jvm
    container_name: skillgateway-server
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${SERVER_PORT:-18080}:8080"
    environment:
      DB_JDBC_URL: jdbc:postgresql://db:5432/skillgateway
      DB_USERNAME: admin
      DB_PASSWORD: changeme
      AUTH_API_KEY: ${AUTH_API_KEY:-dev-api-key}
      AI_EMBEDDING_PROVIDER: ${AI_EMBEDDING_PROVIDER:-ollama}
      AI_EMBEDDING_URL: ${AI_EMBEDDING_URL:-http://ollama:11434/api/embeddings}
      AI_EMBEDDING_MODEL: ${AI_EMBEDDING_MODEL:-nomic-embed-text}
      AI_EMBEDDING_API_KEY: ${AI_EMBEDDING_API_KEY:-}
      AI_EMBEDDING_DIMENSION: ${AI_EMBEDDING_DIMENSION:-768}
      AI_EMBEDDING_TIMEOUT_SECONDS: ${AI_EMBEDDING_TIMEOUT_SECONDS:-3}

  db:
    image: pgvector/pgvector:pg16
    container_name: skillgateway-db
    environment:
      POSTGRES_DB: skillgateway
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: changeme
    ports:
      - "${DB_PORT:-15432}:5432"
    volumes:
      - skillgateway_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d skillgateway"]
      interval: 10s
      timeout: 5s
      retries: 5

  ollama:
    image: ollama/ollama:latest
    container_name: skillgateway-ollama
    ports:
      - "${OLLAMA_PORT:-11435}:11434"
    volumes:
      - skillgateway_ollama:/root/.ollama

volumes:
  skillgateway_pg_data:
  skillgateway_ollama:
